# Camera backend: "picamera2" or "opencv" (auto-detect if unset)
# CAMERA_BACKEND=opencv

# Camera device index for OpenCV (default: 0)
# CAMERA_DEVICE_INDEX=0

# Server host and port
# HOST=0.0.0.0
# PORT=8000

# Frame dimensions
# FRAME_WIDTH=640
# FRAME_HEIGHT=480

# Target FPS for streaming
# STREAM_FPS=15
